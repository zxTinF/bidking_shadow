# 第 4 回合后隐藏金/红区域分解与估值需求文档

## 1. 背景

当前项目已经具备以下能力：

- 解析 `Player.log`
- 还原 `Q1~Q4` 已揭示物品布局
- 根据布局规则估计总格数下界
- 结合地图权重、候选物品库和类别约束估计已知物品价格

当前瓶颈在于：

- 第 4 回合后，角色技能已经揭示完白/绿/蓝/紫
- 剩余未揭示部分只可能是 `Q5/Q6`
- 但“剩余空格到底对应哪些隐藏物品”本身并不唯一
- 因此不能直接把“剩余空格数”乘上均价来估值

本需求的目标，是把“隐藏区域分解”和“隐藏区域估值”从单点猜测改为可验证、可解释、可扩展的候选方案系统。

## 2. 问题定义

输入为第 4 回合时刻的仓库局面：

- 已揭示的 `Q1~Q4` 物品
- 已知的形状、类别、位置、排除约束
- 当前地图 `map_id`
- 地图类别权重 / 掉落权重
- 现有布局运动规则

输出不是单一结论，而是：

- 隐藏区域的候选分解方案集合
- 每个方案的可行性说明
- 每个方案下的隐藏块候选、金/红概率与期望价格
- 方案级总价估计
- 仓库级保守 / 最可能 / 激进估值

## 3. 核心原则

### 3.1 先分解，再估值

必须先回答：

- 剩余隐藏空格对应几个隐藏物品
- 每个物品可能是什么形状
- 这些形状在当前布局规则下是否稳定

只有在完成隐藏区域分解之后，才允许进入 `Q5/Q6` 估值。

### 3.2 不追求单一答案

系统不得假设“第 4 回合局面一定存在唯一隐藏解”。

系统必须承认三种情况：

- 可唯一确定
- 存在多个可行方案
- 只能给出区间，不能给出唯一方案

### 3.3 可证伪优先

算法第一目标不是“猜中”，而是“先删掉不可能的方案”。

所有候选方案必须通过以下校验：

- 几何可放置
- 不与已知 `Q1~Q4` 布局冲突
- 不与已知类别 / 品质 / 道具约束冲突
- 满足本项目归纳出的移动与稳定规则

## 4. 业务约束

### 4.1 角色技能约束

本项目当前对应的角色技能规则：

- 第 1 回合显示白色物品
- 第 2 回合显示绿色物品
- 第 3 回合显示蓝色物品
- 第 4 回合显示紫色物品
- 第 5 回合起不再新增这类角色揭示

因此在第 4 回合后，隐藏物品只能来自：

- `Q5`
- `Q6`

### 4.2 拍卖场景约束

拍卖是逐回合进行的，倍数门槛会下降：

- 第 1 轮：2.0 倍
- 第 2 轮：1.6 倍
- 第 3 轮：1.3 倍
- 第 4 轮：1.1 倍
- 第 5 轮：1.0 倍

因此本需求的主要应用场景，是支持第 4 回合和第 5 回合的拍卖决策。

### 4.3 候选库约束

隐藏区域分解时，只允许使用真实物品表中存在的 `Q5/Q6` 形状：

- 只能从 `item_prices.csv` 中提取 `quality in {5, 6}` 的真实形状
- 不允许生成库中不存在的形状

## 5. 需求目标

### 5.1 第一阶段目标

建立“隐藏区域候选方案系统”的基础框架，支持：

- 识别隐藏开放区域
- 对每个区域枚举可行形状组合
- 对每个组合做稳定性校验
- 输出多个候选方案而不是单点答案

### 5.2 第二阶段目标

在方案层之上增加估值能力：

- 每个隐藏块计算 `P(金)`、`P(红)`、`EV`
- 每个方案输出隐藏总期望价
- 仓库整体输出保守 / 最可能 / 激进估值

### 5.3 第三阶段目标

把方案与估值结果接入 UI：

- 在网格界面展示隐藏区域候选方案
- 展示方案级估值摘要
- 支持切换查看保守 / 最可能 / 激进解

## 6. 非目标

本需求当前不解决以下问题：

- 直接预测对手具体出价
- 还原对手手牌或全部道具使用策略
- 构建完整博弈论最优出价模型
- 在第 1~3 回合就给出高置信度隐藏金/红判断

## 7. 输入输出定义

## 7.1 输入

以单局某一快照为输入，至少包含：

- `GameState`
- `map_id`
- 第 4 回合时已知物品集合
- 物品位置、形状、类别、品质、排除约束
- 当前布局规则推断接口
- 候选物品库与地图权重接口

### 7.2 中间对象

建议引入以下中间对象：

- `HiddenRegion`
  - 一个隐藏连通区域
  - 包含区域格子、边界、开放方向、相邻已知块
- `HiddenBlockCandidate`
  - 区域内一个候选隐藏块
  - 包含形状、位置、稳定性说明
- `HiddenLayoutPlan`
  - 某个区域或全局的隐藏布局方案
  - 包含若干隐藏块、可行性证明、评分
- `HiddenValueEstimate`
  - 某块或某方案的估值结果
  - 包含 `P(Q5)`、`P(Q6)`、`EV`、价格区间

### 7.3 输出

最终输出需要至少包括：

- 全局候选方案列表
- 每个方案的：
  - 隐藏块列表
  - 稳定性校验结果
  - 候选物品统计
  - 总期望价
- 仓库级汇总：
  - 保守总价
  - 最可能总价
  - 激进总价
  - 隐藏部分红色概率摘要

## 8. 算法分层需求

### 8.1 隐藏区域识别层

系统需要先从第 4 回合局面中识别隐藏区域。

区域识别要求：

- 基于 `Q1~Q4` 已知布局与当前总格数推断
- 将未揭示的隐藏部分拆成多个连通区域
- 支持顶部开放区、右侧开放区、中部空腔区、底部阶梯区等不同结构

输出：

- `HiddenRegion[]`

### 8.2 可行分解层

对每个隐藏区域，生成候选分解方案。

方案生成要求：

- 只使用 `Q5/Q6` 真实存在形状
- 允许一个区域包含多个隐藏物品
- 支持多个不同方案并存
- 不要求一次性找到唯一解

剪枝要求：

- 若形状越界则剔除
- 若与已知块重叠则剔除
- 若违反布局稳定规则则剔除
- 若与道具 / 类别 / 品质约束冲突则剔除

输出：

- `HiddenLayoutPlan[]`

### 8.3 稳定性校验层

稳定性校验是本需求的核心难点。

至少需要支持：

- 物品从右向左移动直到被挡住
- 滑动过程中若上方有可插入空位，可优先上插
- 若某候选块在当前状态下仍可整体继续移动，则该摆法不稳定，应剔除
- 若某候选块的存在是阻挡其他块稳定所必需，则需记录该因果关系

校验输出不仅要有真假，还要有解释：

- 为什么稳定
- 为什么不稳定
- 被哪条规则否定

### 8.4 方案评分层

当存在多个可行方案时，需要对方案排序，而不是随意选一个。

评分因素建议包含：

- 地图类别权重匹配度
- 候选物品数量是否集中
- 是否依赖过多低概率形状拼装
- 稳定性条件是否自然
- 是否能解释更多已知布局因果

输出：

- 方案得分
- 排名

### 8.5 估值层

只有通过方案校验后，才进入估值层。

每个隐藏块需要：

- 限定 `quality in {5, 6}`
- 使用形状、类别、地图权重、排除约束查询候选
- 计算：
  - `P(金)`
  - `P(红)`
  - `EV`
  - 最低候选价
  - 最高候选价

每个方案需要：

- 汇总隐藏块估值
- 输出隐藏总期望价
- 输出隐藏红色爆发潜力

## 9. UI 展示需求

现有界面后续需要支持：

- 显示隐藏区域边界
- 切换查看不同候选方案
- 显示每个方案的：
  - 隐藏块结构
  - 方案评分
  - 估值摘要
- 在信息栏或弹窗中显示：
  - 保守总价
  - 最可能总价
  - 激进总价
  - 隐藏红概率

## 10. 验证需求

### 10.1 验证数据来源

验证必须使用现有 `records/*.json` 中带最终揭晓的记录。

用途：

- 对比第 4 回合推断结果与最终真实揭晓
- 检查方案是否包含真实解
- 检查估值区间是否覆盖真实结果

### 10.2 验证指标

至少定义以下指标：

- 真实解命中率
  - 真实隐藏布局是否出现在候选方案集中
- 方案集规模
  - 候选方案数量是否可控
- 保守估值偏差
- 最可能估值偏差
- 激进估值偏差
- 红色识别召回
  - 真实 Q6 是否在高红概率方案中被覆盖

### 10.3 验证要求

阶段性要求：

- 不允许为了命中某几个案例而破坏大量已有记录
- 任何新增规则都必须至少在已有验证记录上回扫
- 对于无法唯一分解的案例，允许输出多个方案，但必须包含解释

## 11. 工程实现建议

建议新增或拆分以下模块：

- `getlog/hidden_regions.py`
  - 隐藏区域识别
- `getlog/hidden_layout.py`
  - 隐藏块枚举与方案生成
- `getlog/hidden_stability.py`
  - 移动 / 上插 / 左移稳定性校验
- `getlog/hidden_value.py`
  - `Q5/Q6` 候选估值
- `getlog/hidden_validation.py`
  - 基于 `records/*.json` 的回扫验证

现有 `grid_view_core.py` 只负责：

- 调用接口
- 汇总结果
- 向 UI 提供展示数据

不应把整套隐藏区域逻辑继续堆回单文件。

## 12. 里程碑

### M1：区域与方案框架

- 识别隐藏区域
- 定义方案对象
- 输出多个候选方案

### M2：稳定性规则落地

- 左移规则
- 上插规则
- 阻挡因果规则

### M3：估值能力落地

- `Q5/Q6` 候选估值
- 方案级总价汇总
- 保守 / 最可能 / 激进估值

### M4：验证与 UI

- 全记录回扫
- 命中率与偏差统计
- UI 展示接入

## 13. 当前结论

该问题不是“剩余空格值多少钱”，而是：

- 剩余空格对应哪些隐藏物品块
- 这些隐藏物品块是否在当前局面下稳定
- 在多个可行方案中，哪些方案更可信
- 在可信方案集合上，隐藏金/红的期望价值是多少

因此本项目后续必须从“单点估值”升级为“方案集合 + 方案估值”的建模方式。
